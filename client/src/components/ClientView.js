// Componente para vista cliente
import React from 'react';

const ClientView = () => {
  return (
    <section id="cliente-view" className="container mt-5 hero">
      <h2 className="text-center mb-4">Bienvenido, Cliente</h2>
      <p className="text-center">Placeholder: Tarjetas de productos con fetch de API pronto.</p>
    </section>
  );
};

export default ClientView;